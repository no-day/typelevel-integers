/**
 * It's a module!
 *
 * @since 1.0.0
 * @example
 *   console.log('hello');
 */

/**
 * It's a module!
 *
 * @since 1.0.0
 * @example
 *   console.log('hello');
 */
export type As<E, T> = T extends E ? T : never;

/**
 * It's a module!
 *
 * @since 1.0.0
 * @example
 *   console.log('hello');
 */
export type Returns<T, H extends T> = H;
